services:
  mip-api:
    build: .
    command: ["serve", "--db", "/data/mip.db", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./mfcode:/mfcode:ro
