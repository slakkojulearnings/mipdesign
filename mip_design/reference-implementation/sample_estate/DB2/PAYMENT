-- PAYMENT table definition (DB2 DDL).
CREATE TABLE PAYMENT (
    PAYMENT_ID    INTEGER       NOT NULL GENERATED ALWAYS AS IDENTITY,
    CARD_NUMBER   CHAR(16)      NOT NULL,
    PAY_AMOUNT    DECIMAL(9,2)  NOT NULL,
    PRIMARY KEY (PAYMENT_ID)
);
