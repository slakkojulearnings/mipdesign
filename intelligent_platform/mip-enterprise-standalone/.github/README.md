# GitHub Workspace

This standalone package includes:

- `copilot-instructions.md` for GitHub Copilot Chat.
- `workflows\ci.yml` for backend tests and frontend build.

No model API keys are required by default. Add provider-specific secrets only if
you later add GitHub Actions that call GPT or Anthropic APIs.
