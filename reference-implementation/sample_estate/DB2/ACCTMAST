-- ACCT_MASTER table definition (DB2 DDL).
CREATE TABLE ACCT_MASTER (
    CARD_NUMBER   CHAR(16)      NOT NULL,
    BALANCE       DECIMAL(11,2) NOT NULL,
    CREDIT_LIMIT  DECIMAL(11,2) NOT NULL,
    PRIMARY KEY (CARD_NUMBER)
);
