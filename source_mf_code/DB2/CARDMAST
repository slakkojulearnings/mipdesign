-- CARD_MASTER table definition (DB2 DDL).
CREATE TABLE CARD_MASTER (
    CARD_NUMBER       CHAR(16)      NOT NULL,
    CARD_STATUS       CHAR(01)      NOT NULL,
    CURRENT_BALANCE   DECIMAL(11,2) NOT NULL,
    CUST_NUMBER       CHAR(10)      NOT NULL,
    PRIMARY KEY (CARD_NUMBER)
);
